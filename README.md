# Air Remote

Пульт для Android TV, Bluetooth-клавиатура и трекпад для ПК, Wi-Fi-микрофон для Windows.

## Настройка

### Телевизор

1. Подключите телефон и Android TV к одной локальной сети.
2. Во вкладке **ТВ** откройте настройки кнопкой с ползунками и выберите телевизор.
   Если поиск не нашёл ТВ, введите его IP-адрес из сетевых настроек и нажмите **Сопряжение**.
3. Введите шестизначный код с экрана телевизора.

Громкость меняется боковыми кнопками телефона. Клавиатура и голос доступны
кнопками внизу пульта; для голоса нужно разрешение на микрофон.

Кнопка питания сразу отправляет ИК-команду Haier. Направьте ИК-передатчик
телефона на телевизор. Проверять и сохранять профиль в приложении не нужно.
Без ИК-передатчика кнопка отправляет питание по Wi-Fi на подключённый Android TV;
удержание передаёт долгое нажатие. Универсального подбора ИК-кодов нет.

### Компьютер

Для клавиатуры и трекпада сервер на ПК не нужен — только Bluetooth.
Пошаговое сопряжение и жесты: [ПК: Bluetooth-мышь и клавиатура](#пк-bluetooth-мышь-и-клавиатура).
Видимость телефона включается кнопкой **Видимость · 2 минуты** в списке устройств.
RU/EN меняет подписи клавиш; язык ввода на ПК переключается сочетанием Win + Space.

### Микрофон

Установите на Windows AndroidMic и VB-CABLE, затем укажите IP-адрес ПК и порт
во вкладке **Микрофон**. [Пошаговая настройка Windows](docs/microphone.md#первый-запуск).

Режим **Естественный звук** не добавляет программное усиление и запрашивает
необработанный вход; **Речь** использует обработку для разговоров, доступную на телефоне.
Запись продолжается при блокировке экрана. Остановить её можно в приложении
или уведомлении. **Без звука** сохраняет соединение и передаёт тишину.


## Особенности протокола

- Схемы `polo.proto` и `remotemessage.proto` генерируются Gradle при сборке. Не редактируйте файлы в `app/build/generated`.
- Телефон и телевизор должны быть в одной сети. Сопряжение использует шестизначный код с экрана ТВ; сертификат телевизора закрепляется после проверки кода.
- Голосовой ввод передаёт моно PCM 8 кГц и требует разрешение на микрофон.

## Клавиатура

- При запросе ввода от ТВ открывается клавиатура телефона с текстом и выделением активного поля. Её также можно открыть кнопкой «Клавиатура» на пульте.
- Правки передаются сразу: набор, вставка, удаление, очистка и перемещение курсора. Кнопка действия учитывает поиск, переход к следующему полю и другие действия редактора ТВ.
- Правки и действие «Ввод» используют режим совместимости с нулевыми счётчиками IME: на проверенном ТВ возврат счётчиков из уведомлений приводил к игнорированию ввода в активное поле. Очередь сохраняет порядок команд. При смене поля или соединения устаревшие правки отбрасываются.
- Автоматическое открытие и получение текста зависят от того, передаёт ли приложение ТВ состояние поля через Android TV Remote Service. Для полей без такого состояния доступна ручная отправка текста.
- В `RemoteEditInfo` описаны вложенные операции замены диапазона, выделения и действия редактора. Позиции задаются в UTF-16; исходная схема `insert` не описывала операции выделения и действия редактора.

## ИК-питание

Питание по ИК работает только на телефонах с ИК-передатчиком. Встроен профиль Haier; команда отправляется сразу, независимо от Wi-Fi-сопряжения. Без ИК используется питание Android TV по Wi-Fi (включая удержание); требуется доступное соединение с ТВ.

## Проверка

```sh
./gradlew lintDebug testDebugUnitTest assembleDebug
```

На устройстве: открыть заполненное поле поиска на ТВ, проверить автоматическое открытие и текст на телефоне, вставить и удалить текст в середине строки, очистить поле, выполнить поиск. Затем закрыть экран клавиатуры и открыть его кнопкой пульта; повторить после переподключения.

## Источники протокола

- [androidtvremote2](https://github.com/tronikos/androidtvremote2) — Apache-2.0; текст лицензии: `THIRD_PARTY_LICENSE_androidtvremote2`.
- [Google TV pairing protocol](https://android.googlesource.com/platform/external/google-tv-pairing-protocol/).

## ПК: Bluetooth-мышь и клавиатура

Режим **ПК** открывается вкладкой или свайпом влево. Свайпы в свободных областях переключают ТВ → ПК → Микрофон → ТВ по кругу, в обе стороны. Жесты на трекпаде и элементах, обрабатывающих движение пальца, не переключают режим.

Используется системный [BluetoothHidDevice API](https://developer.android.com/reference/android/bluetooth/BluetoothHidDevice) Android 9+: телефон объявляет составное HID-устройство с клавиатурой и мышью. На Windows приложение или сервер не требуется; нужен Bluetooth на ПК и поддержка HID Device прошивкой телефона.

1. В режиме ПК нажать **Подключить** и разрешить доступ к устройствам поблизости.
2. Включить Bluetooth, дождаться готовности HID и нажать **Видимость · 2 минуты**.
3. В Windows открыть **Параметры → Bluetooth и устройства → Добавить устройство → Bluetooth**, выбрать телефон и подтвердить код на обоих устройствах.
4. При необходимости выбрать сопряжённый ПК в списке Air Remote. Последний подключённый компьютер запоминается для повторного подключения при регистрации HID.

Одновременно HID может занимать только одно приложение. Если открыт другой Bluetooth-пульт, его нужно закрыть и повторить подключение. После использования другой HID-программы Windows может хранить прежнее описание устройства: при проблемах удалить сопряжение на обоих устройствах и выполнить его заново с Air Remote.

- Один палец двигает курсор, касание выполняет левый щелчок; два пальца прокручивают, касание двумя пальцами выполняет правый щелчок.
- Удержание пальца и движение включает перетаскивание; отпускание пальца отпускает кнопку мыши.
- Клавиатура отправляет физические клавиши, включая F1–F12, стрелки и сочетания. Shift расположен по краям буквенного ряда, стрелки — отдельным блоком «перевёрнутая Т» ниже. Ctrl, Alt, Shift и Win выбираются для следующей клавиши, затем сбрасываются. Отдельная Win слева внизу открывает «Пуск» одним нажатием.
- RU/EN меняет подписи; раскладка ввода определяется Windows. Для переключения языка Windows нажать Win, затем Space.
- Приложение нужно держать открытым: Android может снять регистрацию HID при уходе в фон. При выходе из режима ПК или сворачивании отправляется отпускание кнопок и клавиш. Возврат в приложение повторно регистрирует HID, если требуется.

## Микрофон для Windows по Wi-Fi

Вкладка **Микрофон** передаёт звук в Windows-приёмник AndroidMic: 48 кГц, моно,
PCM Float32 (PCM16 при отсутствии поддержки), без сжатия. Есть естественный
и речевой режимы, индикатор уровня, mute и фоновая работа с остановкой из
уведомления. Bluetooth-клавиатура и мышь могут работать одновременно.

На ПК нужны AndroidMic и VB-CABLE. Настройка, выбор транспорта, ограничения
и разбор upstream: [docs/microphone.md](docs/microphone.md).

## Структура кода

- `MainActivity` — разрешения, жизненный цикл и аппаратные кнопки громкости.
- `RemoteModes` — вкладки и переключение режимов.
- `TvScreen`, `RemoteControls` — экран ТВ и кнопки пульта.
- `PcScreen`, `PcKeyboard`, `Trackpad` — подключение ПК и независимые элементы ввода.
- `RemoteModel`, `PcRemoteModel` — состояние подключения и команды устройств.
- `TvClient`, `TvKeyboard`, `PcHidOutput`, `PcHidReports`, `IrSignal` — протоколы и очереди команд.
- `MicrophoneScreen`, `MicrophoneService`, `MicTransport` — управление, фоновая запись и транспорт аудио.

Для сборки нужны JDK 17 или новее и Android SDK 37. Путь к SDK задаётся через
`ANDROID_HOME` либо `sdk.dir` в локальном `local.properties`.

## Production APK

```sh
bash scripts/build-release.sh
```

Подписанный APK: `app/build/outputs/apk/release/app-release.apk`.
Первый запуск создаёт ключ в `.signing/` (вне Git): сохрани резервную копию папки для будущих обновлений.
Дальше работает и `./gradlew assembleRelease`. Повышай `versionCode` перед выпуском обновления.
Release не обновит установленную debug-версию с другой подписью: переход требует удаления debug-приложения с потерей его настроек.
