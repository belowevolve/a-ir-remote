# Air Remote

Пульт для Android TV по локальной сети и ИК-передатчику телефона.

## Особенности протокола

- Схемы `polo.proto` и `remotemessage.proto` генерируются Gradle при сборке. Не редактируйте файлы в `app/build/generated`.
- Телефон и телевизор должны быть в одной сети. Сопряжение использует шестизначный код с экрана ТВ; сертификат телевизора закрепляется после проверки кода.
- Голосовой ввод передаёт моно PCM 8 кГц и требует разрешение на микрофон.

## Клавиатура

- При запросе ввода от ТВ открывается клавиатура телефона с текстом и выделением активного поля. Её также можно открыть кнопкой «Клавиатура» на пульте.
- Правки передаются сразу: набор, вставка, удаление, очистка и перемещение курсора. Кнопка действия учитывает поиск, переход к следующему полю и другие действия редактора ТВ.
- Правки и действие «Ввод» используют режим совместимости с нулевыми счётчиками IME: на проверенном ТВ возврат счётчиков из уведомлений приводил к игнорированию ввода в активное поле. Очередь сохраняет порядок команд. При смене поля или соединения устаревшие правки отбрасываются.
- Автоматическое открытие и получение текста зависят от того, передаёт ли приложение ТВ состояние поля через Android TV Remote Service. Для полей без такого состояния доступна ручная отправка текста.
- В `RemoteEditInfo` описаны вложенные операции замены диапазона, выделения и действия редактора. Позиции задаются в UTF-16; исходная схема `insert` не описывала операции выделения и действия редактора.

## ИК-питание

Питание по ИК работает только на телефонах с ИК-передатчиком. Профиль сохраняется отдельно от подключения к телевизору; сначала проверьте команду в настройках.

## Проверка

```sh
./gradlew lintDebug testDebugUnitTest assembleDebug
```

На устройстве: открыть заполненное поле поиска на ТВ, проверить автоматическое открытие и текст на телефоне, вставить и удалить текст в середине строки, очистить поле, выполнить поиск. Затем закрыть экран клавиатуры и открыть его кнопкой пульта; повторить после переподключения.

## Источники протокола

- [androidtvremote2](https://github.com/tronikos/androidtvremote2) — Apache-2.0; текст лицензии: `THIRD_PARTY_LICENSE_androidtvremote2`.
- [Google TV pairing protocol](https://android.googlesource.com/platform/external/google-tv-pairing-protocol/).
